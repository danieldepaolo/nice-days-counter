import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "3rem"
    },
    h2: {
      fontSize: "2.5rem"
    },
    h3: {
      fontSize: "2.05rem"
    },
    h4: {
      fontSize: "1.65rem"
    },
    h5: {
      fontSize: "1.4rem"
    },
    h6: {
      fontSize: "1.2rem"
    }
  }
})

export default theme
